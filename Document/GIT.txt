Add git global settings
$ git config --global user.name "patra.sa"

remote: Hint: To automatically insert Change-Id, install the hook:
remote:   gitdir=$(git rev-parse --git-dir); scp -p -P 29418 patra.sa@gerrit.pg.com:hooks/commit-msg ${gitdir}/hooks/
remote: And then amend the commit:
remote:   git commit --amend

To PUSH Changes
git push origin master:refs/for/master

To Get sync with remote repo changes
$ git pull
Already up to date.

$ git fetch --all
Fetching origin

To Be in EXACT sync with remote changes
$ git reset --hard origin/master

echo "Apple Pie" | git hash-object --stdin
23991897e13e47ed0adb91a0082c31c82fe0cbe5  -> This isi SHA-1 For String "Apple pie"

git cat-file -p SHA-1_code -> This will give details and content

$ echo "Apple Pie" | git hash-object --stdin -w
fatal: Not a git repository (or any of the parent directories): .git

$ mkdir gittest

$ cd gittest/

$ git init - > TO create a empty git repository
Initialized empty Git repository in C:/PG Project/gittest/.git/

git add <File_List> -> to Move to staging area

git status -> To check git status

$ git commit -m "First Commit"

$ git log -> Gives you log history based on recent command up.


